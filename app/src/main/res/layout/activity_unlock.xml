<?xml version="1.0" encoding="utf-8"?>
<!--
    Unlock screen layout — redesigned from a single-message card to a
    full-screen chat list, matching the style of activity_conversation.xml.
    The toolbar shows the contact name; the ListView shows all decrypted
    messages from that sender after biometric auth succeeds.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/unlockToolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/brand_primary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Shown while auth is pending / messages are being decrypted -->
    <TextView
        android:id="@+id/unlockWaitingText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="24dp"
        android:text="@string/waiting_auth"
        android:textColor="@color/text_hint"
        android:textSize="14sp"
        android:visibility="gone" />

    <ListView
        android:id="@+id/unlockMessageList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:clipToPadding="false"
        android:divider="@null"
        android:dividerHeight="10dp" />

</LinearLayout>